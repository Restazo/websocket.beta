class VisitorConnection {
  constructor(table_id, tableNumber, business_id) {
    this.table_id = table_id;
    this.tableNumber = tableNumber;
    this.business_id = business_id;
  }
}

export default VisitorConnection;
